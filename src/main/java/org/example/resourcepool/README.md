## Semaphore-based Connection Pool

Implement a database connection pool using Semaphore.  
Requirements:
- Fixed pool size
- Acquire/release connections
- Block when no connections available
- Support timeout for acquiring connection
- Handle connection validation
- Track available and used connections

Key Concepts: Semaphore, resource pooling, timeout handling