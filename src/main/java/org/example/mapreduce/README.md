## Parallel Aggregate Operations

Implement parallel aggregate operations (map-reduce style) on a collection.

Requirements:
- Split collection into chunks for parallel processing
- Apply transformation (map) in parallel
- Combine results (reduce) correctly
- Handle exceptions in worker threads
- Use ForkJoinPool or custom thread pool

Key Concepts: Map-reduce, parallel aggregation, combining functions